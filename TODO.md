tabler icons
card
flex
stack
group
box
form based components radio, checkbox, input, textarea etc
button
center

date components eg date picker, date time picker

also know the props that are passed to mantine components eg hiddenFrom, p, m, px, my etc, this goes for props that all mantine components accept and the props that are tied to specific components eg Flex component has a direction prop, a SimpleGrid component has a cols prop etc

have a landing page for the event site then have an actual web app (dashboard with appshell) for when a user signs in, do not implement the auth, the key is learning mantine so you can mock the auth functionality

know how to guard the pages with the mocked auth, ie a user should not see protectedpages if they are not logged in, hint proxy.ts - formerly middleware.ts , layout.tsx
